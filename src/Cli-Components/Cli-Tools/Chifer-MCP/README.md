# Chifer MCP Tools

This folder contains runtime tools for fast local memory retrieval.

## Behavior
- Uses local markdown memory as source data.
- Runs zero external API calls for memory retrieval.
- Applies keyword-based matching from prompt to memory lines.
- Emits live activity messages for terminal UI.
